import SlideSection from "../slide-section";
import SideNav from "../side-nav";

const Home = () => {
  return (
    <div>
      <SlideSection />
      <SideNav />
    </div>
  );
};

export default Home;
